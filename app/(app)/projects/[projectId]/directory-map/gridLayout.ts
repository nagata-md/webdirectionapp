// ページ名とタグ類を「続き書き」ではなく列として揃えて表示するための共通グリッド定義
// （2026-08-07新規要件）。ヘッダー行・各ページ行の両方で同じ列幅を使うことで、
// 何階層ネストしていても種別・複雑度・各種タグの列が縦に揃って見える。
// 「優先度」列は2026-08-07に廃止（進行グループの数値と紛らわしいとの指摘を受け、
// 表示順は進行グループの並び順のみに一本化。手動での細かい調整はドラッグ&ドロップのみで行う）。
// 最終列（操作）は2026-08-07修正：`auto`のままだとヘッダーの短いラベル「操作」と
// 実際の行の「編集・削除」ボタン幅が異なり、各行ごとに独立したgridコンテナのため
// auto列の実測幅が食い違って手前の列がズレて見える不具合があった。固定幅にして解消。
export const ROW_GRID_COLS =
  "grid-cols-[24px_minmax(170px,1.6fr)_60px_44px_70px_70px_70px_78px_84px_104px_120px_152px]";

export const ROW_GRID_CLASS = `grid w-full ${ROW_GRID_COLS} items-center gap-x-2 gap-y-1`;
