# 制作進行オートメーター — spec.md (v1)

作成日：2026年8月1日
ベース文書：`要件定義書_制作進行オートメーター.md`（v0.2）

---

## 0. このドキュメントについて

これは Claude Code で実装するための仕様書（v1）。**「⚠️ 要相談」** が付いた箇所は、実装着手前に人間が決める。それ以外は本書の記述を正とする。

要件定義書にない追加要件として、**Figmaリンクの登録**（§4.1）と**AIによるメタ情報一括生成**（§4.9）を本書で新規に定義する。

技術スタックは要件定義書では未規定だったが、本書で **Next.js + Supabase + Vercel** に確定する（§7）。デザインは既存の `DESIGN_SYSTEM.md`（名刺情報共有ツール／サーバー情報管理アプリで使用の素HTML/CSS前提デザインシステム）のトーンを Tailwind に移植して踏襲する（§12）。

---

## 1. プロダクト概要

### 背景

Webサイト制作案件（ディレクトリマップ設計・ワイヤー／コピー／デザイン／コーディング・スケジュール管理・見積もり作成）をスプレッドシートで手作業運用しており、属人化・工数増大が発生している。Claude内で作成したHTMLプロトタイプ（v0.5）で設計を検証済み。

### コンセプト

ページ単位の複雑度・要否フラグから見積もりとスケジュールを自動算出し、進行グループ単位で段階的に着手するWebサイト制作案件を、ディレクトリマップ・スケジュール・見積もり・メタ情報を1つの台帳で一元管理するツール。

### 提供価値

- **社内**：ディレクトリマップ・スケジュール・見積もりの手作業を排し、単価マスタ変更や構成変更に強い運用にする。
- **クライアント／外部スタッフ**：見積もり・ディレクトリマップ・スケジュールを、社外秘のマスタ単価を除いた形で安全に共有できる。

---

## 2. ユーザーとロール

| ロール | 説明 | 認証 |
| :---- | :---- | :---- |
| チームメンバー | マスタ設定・全プロジェクトを閲覧・編集できる（本フェーズでは権限分離なし） | 必要（Google OAuth、`marketingdept-llc.com` ドメイン制限） |
| 外部閲覧者（クライアント／外部スタッフ） | 発行された共有URL経由で、見積もり・ディレクトリマップ・スケジュールを参照専用で閲覧 | 不要（共有URL＋任意パスワード） |

- 本フェーズでは編集権限の細分化（マスタは管理者のみ編集可、等）は行わない（要件定義書§5・masterplan相当の次フェーズ課題として持ち越し）。
- 単価マスタは社外秘のため、ログイン必須化を非機能要件とする（§8）。

---

## 3. 用語定義

| 用語 | 定義 |
|---|---|
| プロジェクト | 1つのWebサイト制作案件。ページ群・スケジュール・見積もりを内包する単位 |
| ページ | ディレクトリマップ上の1ページ（TOP、下層、LP、ブログ等） |
| 複雑度（S/M/L） | ページの制作難易度ランク。標準工数・単価の算出基準 |
| コスト工程 | 単価が発生する作業単位。ワイヤー／コピー／デザイン／コーディング／テストアップ／公開の6種 |
| スケジュール工程 | ガントチャート上の期間単位。構成（ワイヤー＋コピー）／デザイン／コーディング／テストアップ／公開の5種 |
| 進行グループ | 同じタイミングで着手するページの集合。優先度の高い順に段階的に着手する（開始条件は§4.3で確定） |
| マスタ設定 | 全プロジェクト共有の単価・工数・標準待機日数・休日カレンダー・AI連携設定等 |
| チェックバック（CB） | クライアントによる確認・返信待ちの期間 |
| バッファ | 自社都合の安全余白期間 |
| 手動オーバーライド | 自動計算されたガントチャートの日付を人が手動で上書きした状態 |

---

## 4. 主要機能

### 4.1 プロジェクト管理

| 項目 | 内容 |
|---|---|
| 機能 | プロジェクトの作成・切替・削除・基本情報編集 |
| 保持する基本情報 | クライアント名、プロジェクト名、自社担当者（ディレクター／デザイナー／コーダー等の役割別に複数登録可）、着手日、**サーバー情報リンク**（本番URL・ステージングURL・FTP/CMS管理画面等への外部リンクを複数登録可、ラベル付き）、**Figmaリンク（新規要件・複数登録可、ラベル付き）** |
| バリデーション | プロジェクト名は必須。最後の1件は削除不可 |
| 備考 | サーバー情報リンク・Figmaリンクとも機密情報や実データそのものは保持せず、外部サービス（社内共有ドライブ・Figma）への「リンク」のみを保持する。Figma APIとの自動連携（サムネイル取得・データ同期等）は行わない（要件定義書§4.2のスコープ外方針を維持し、単なる外部リンク登録に留める） |

**サーバー情報リンク／Figmaリンクの実装方針**：データ構造は同一（`{ label, url }` の配列）とし、`category`（`server` / `figma`）で分けて1つのテーブルに保持する（§6）。UI上はプロジェクト基本情報画面で「サーバー情報リンク」「Figmaリンク」の2セクションに分けて表示・追加する。

### 4.2 ディレクトリマップ

| 項目 | 内容 |
|---|---|
| 機能 | ページの追加・編集・削除、親子階層設定、ツリー表示 |
| ページ項目 | ページ名、種別（TOP/下層/LP/ブログ/その他）、複雑度（S/M/L）、親ページ、ワイヤー要否、コピー要否、個別費用（円）、進行グループ、優先度 |
| 優先度の向き（確定） | 数値が小さいほど優先度が高い（1が最優先）。ディレクトリマップのツリー表示・スケジュール自動生成（§4.5）とも、この昇順で処理する |
| ツリー表示 | 親子関係に基づき階層インデントで表示。ワイヤー／コピー不要ページ、進行グループはバッジで視覚化 |

### 4.3 進行グループ管理

- プロジェクトごとに複数の「進行グループ」を定義できる（例：グループ1＝TOP＋主要下層、グループ2＝事業A、グループ3＝事業B）。

| 項目 | 内容 |
|---|---|
| グループ名 | 任意の名称 |
| 表示順 | グループの着手順序（1, 2, 3…） |
| 所属ページ | ディレクトリマップ上の各ページに1つの進行グループを割り当てる（未設定ページは表示順1のデフォルトグループ扱い） |

**開始条件（確定・要件定義書§6.3の未決定事項#1への回答）**：

- **表示順1のグループ**：プロジェクト開始日を起点とする。
- **表示順2以降のグループ**：直前の表示順のグループに属する全ページの「構成」（スケジュール工程）が完了した日を、そのまま起点とする（追加のオフセット日数は挟まない）。「構成完了日」は、そのグループに属する全ページの構成工程終了日のうち最大値。

> ⚠️ 実装上の注意：グループ2以降の起点は前グループの計算結果に依存するため、進行グループは表示順に従って**逐次計算**する（並列計算不可）。あるグループのページ構成・複雑度を変更すると、後続グループ全体の再計算が必要になる。

**手動オーバーライドとの相互作用（確定）**：

- グループの「構成完了日」（＝次グループの起点）は、そのグループに属する全ページの構成工程の**実効終了日**（手動オーバーライドがあれば`override_end`、なければ自動計算値`computed_end`）の最大値とする。つまり手動オーバーライドは次グループの起点計算にそのまま反映される（オーバーライドされた日付が「本当の完了日」として扱われる）。
- あるページの構成工程の終了日をオーバーライドし、それが所属グループの構成完了日（最大値）を変化させる場合、次グループ以降の自動計算区間（オーバーライドされていない区間のみ）を再計算するかどうかを、§4.6の後続工程追従ダイアログと同様の確認ダイアログで選択させる（「次グループ以降の自動計算区間を再計算する」／「このページの区間だけ変更する（次グループ以降は今のまま据え置く）」）。
- 上記いずれを選んだ場合も、次グループ以降で**既にオーバーライドされている区間**は変更しない（§4.6の原則を維持。既存オーバーライドは常に自動再計算の対象外）。

### 4.4 マスタ設定（チーム共有）

| 項目 | 内容 |
|---|---|
| 単価・工数マスタ | 複雑度（S/M/L）×コスト工程（ワイヤー／コピー／デザイン／コーディング／テストアップ／公開）ごとの標準日数・単価 |
| 標準待機日数 | スケジュール工程（構成／デザイン／コーディング／テストアップ／公開）ごとのチェックバック日数・バッファ日数 |
| 休日カレンダー | 祝日・特別休業日のリスト（日付・名称、追加/削除可） |
| 定休日 | 曜日指定による毎週の除外日（デフォルト：土日） |
| 月額ディレクション費 | 円／月。プロジェクトのスケジュール期間（開始月〜完了月の暦月数）×月額で見積もりに計上 |
| 工程別並行作業人数のデフォルト値 | プロジェクト作成時の初期値として使用（プロジェクトごとに上書き可） |
| 消費税率 | 見積もりの税込計算に使用。設定可能 |
| **発行元情報（新規）** | 見積書PDFに記載する自社情報（会社名・住所・電話番号・角印画像）、見積書の有効期限日数（既定30日）。§4.11 |
| **AI連携設定（新規）** | Claude APIキー（社外秘、1件のみ登録）、使用モデル名（未指定時は実装時点の推奨モデルを既定値とする） |

### 4.5 スケジュール自動生成ロジック

1. §4.3の開始条件に基づき、進行グループを表示順に沿って逐次処理し、各ページの初期レディ時刻（工程着手可能日＝所属グループの起点日）を決定する。
2. ページは優先度順に処理し、スケジュール工程（構成→デザイン→コーディング→テストアップ→公開）ごとに、工程別の並行作業レーン（プロジェクトで設定した並行作業人数）に空きが出次第割り当てる。
3. 各工程の制作日数は、該当ページの複雑度・ワイヤー/コピー要否に基づきマスタから算出し、営業日ベースで日数を積む。
4. 制作完了後、マスタの標準待機日数（チェックバック＋バッファ）を営業日で加算し、次工程のレディ時刻とする。
5. 全ページの最終工程（公開）完了日のうち最大値をプロジェクト全体の完了予定日とする。

**端数日の扱い（確定）**：単価マスタの日数は0.5日単位で入力できるが、ガントチャート上の開始日・終了日は暦日（丸日）単位のため、スケジュール工程ごとに合計してから切り上げる（構成はワイヤー＋コピーの日数を先に合算してから切り上げる。他の工程は単体で切り上げる）。工程間で端数を繰り越すことはしない。

### 4.6 ガントチャートの表示・手動編集

| 項目 | 内容 |
|---|---|
| 表示 | ページ単位の行×時系列のガントチャート。工程ごとに色分けし、制作期間と待機期間を区別して表示 |
| 手動編集 | 各ページ・各工程の開始日／終了日をドラッグ操作または日付入力で直接変更できる |
| オーバーライド管理 | 手動編集された工程区間は「手動オーバーライド」フラグを立てて記録する。以降、マスタ単価・工数の変更やページ構成の変更があっても、オーバーライド済みの区間は自動再計算で上書きしない |
| 連動編集 | 手動編集によりあるページのある工程が後ろ倒しになった場合、同一ページの後続工程を追従して自動的に後ろ倒しするかどうかを、編集時に確認ダイアログで選択できる（「後続工程も追従させる」／「この区間だけ変更する」） |
| リセット | 手動オーバーライドを解除し、自動計算値に戻す操作を用意する（ページ単位・工程単位の両方で可能） |
| 変更履歴 | 誰が・いつ・どの区間を手動編集したかの履歴を最低限のログとして保持する |
| グループ起点への影響 | 「構成」工程のオーバーライドが所属グループの完了日（＝次グループの起点）を変化させる場合の挙動は§4.3で規定する（次グループ以降の自動計算区間を再計算するか確認ダイアログで選択、既存オーバーライドは対象外） |

### 4.7 見積もり

**表示順（確定）**：画面・CSV・PDFとも、上から「①ディレクション費 → ②ページ別コスト（制作関連） → ③追加項目（素材費〜値引き、手入力）」の順で表示する。合計計算自体は加算なので順序に依存しないが、体裁上はこの並びを正とする。

| 項目 | 内容 |
|---|---|
| ①ディレクション費 | 月額ディレクション費 ×（スケジュール期間の暦月数） |
| ②ページ別コスト | 複雑度別単価 ×（ワイヤー/コピー要否）＋個別費用 |
| ③追加項目（新規要件） | プロジェクトごとに「項目名・金額」の手入力行を任意の件数追加できる。素材費等の追加費用はプラス、値引きはマイナスの金額で入力する（正負自由入力、種別の選択欄は設けない） |
| 合計 | ディレクション費＋制作費小計＋追加項目の合計＝税抜合計、税込合計（消費税率はマスタ設定で変更可能） |
| 出力 | 画面表示に加え、CSV等の形式でエクスポート可能（①→②→③の順、追加項目も内訳に含める） |
| バージョン管理・PDF発行 | 「確定してPDF発行」操作で押下時点の計算結果を凍結・発行する。詳細は§4.11 |

### 4.8 メタ情報・進行管理

| 項目 | 内容 |
|---|---|
| 管理項目 | URLスラッグ、TITLE、ディスクリプション、キーワード、優先度、納品予定日、進捗ステータス（未着手／構成中／デザイン中／コーディング中／テスト中／公開済） |
| 連動 | ディレクトリマップのページと1対1で連動する（同一ページIDを参照） |
| 納品予定日の参考表示（新規要件） | 納品予定日の入力欄の近くに、スケジュール自動計算（§4.5）による「公開」工程の完了予定日を参考情報として表示する。**自動での上書きは行わない**——納品予定日はクライアントへの約束日、進捗ステータスは実態を表す欄であり、内部スケジュールの変動と必ずしも一致しないため、あくまで参考表示に留め、値自体は引き続き手動入力・保存とする |

### 4.9 AIによるメタ情報一括生成（新規要件）

ページ数が多い案件でSEOメタ情報（スラッグ・TITLE・ディスクリプション・キーワード）を手入力する手間を削減する。

| 項目 | 内容 |
|---|---|
| 実行画面 | メタ情報・進行管理画面に「AI一括生成」パネルを設置 |
| 入力 | サイト全体の方向性・トーンを指示する自由記述の指示文（例：「BtoB向け、堅めのトーン、地域名を意識したキーワードを入れる」）。ページごとの個別入力は不要——ディレクトリマップに登録済みの全ページ情報（ページ名・種別・親子階層・優先度）を自動的に読み込み、指示文と合わせてプロンプトに含める |
| 指示文の記憶（新規要件） | 画面を開いた際、指示文欄には当該プロジェクトで直近に使用した指示文を初期値として表示する（同じ方向性を毎回入力し直す手間を省く。生成ログ（下記）の最新1件から取得し、専用の保存項目は設けない） |
| 生成対象の選択 | 「未入力ページのみ」（デフォルト）／「全ページ（既存値も上書き）」を切り替え可能。デフォルトは未入力のみとし、手動で書いたメタ情報を誤って上書きしない（§4.6の手動オーバーライド尊重の方針と一貫させる） |
| 実行 | 「生成」ボタン押下でClaude APIに1回のリクエストを送り、各ページの slug / title / description / keywords を構造化出力（JSON）として受け取る |
| レビュー | 生成結果は即保存せず、ページ別の編集可能なプレビュー一覧に表示する。内容を確認・修正した上で「反映」ボタンで台帳に保存する |
| ログ | 誰が・いつ・どんな指示文で・何ページ分生成したかを最低限記録する（監査目的ではなく、後から経緯を追える程度） |
| 設定 | Claude APIキー・使用モデルはマスタ設定で管理（§4.4） |
| スコープ外 | ページ本文・ワイヤー・コピー・デザインの自動生成は行わない（要件定義書§4.2のスコープ外方針を踏襲。あくまでSEOメタ情報のみ） |

> ⚠️ 要相談：Claude APIの利用コスト（月間見込み回数・トークン量）は実装後に実測し、想定利用規模に対して妥当かを確認する。

### 4.10 外部共有機能（公開URL発行）

社内マスタ（単価・工数表）を除いた、基本情報・見積もり・ディレクトリマップ・スケジュール・メタ情報をクライアントや外部スタッフに閲覧共有できる機能。

| 項目 | 内容 |
|---|---|
| 発行単位 | プロジェクト単位で共有URLを発行する |
| 公開範囲（デフォルト） | 見積もり（総額・ページ別内訳）、ディレクトリマップ（ページツリー）、スケジュール（ガントチャート）。マスタ設定は常に非公開 |
| 公開範囲（除外を推奨する内部運用項目） | ページの個別費用の内訳算出根拠、優先度、進行グループの並行作業人数設定、休日カレンダー、内部担当者名、進捗ステータス（社内管理用の詳細）は、含めた各セクション内で常に非表示とする（個別のON/OFFトグルは設けない）。共有作成時にON/OFF選択できるのは①基本情報②見積もり③ディレクトリマップ④スケジュール⑤メタ情報の5セクション単位（基本情報は下記2段階） |
| 基本情報セクション（新規要件） | 2段階の公開レベルから一方を選んで含める。**①秘密情報なし**：プロジェクト名・クライアント名・着手日のみを表示。**②秘密情報あり（開発サポート者向け）**：①に加えて自社担当者・サーバー情報リンク・Figmaリンクを含む全項目を表示する。両方を同時に含めることはできない（発行画面でどちらか一方のみ選択） |
| 表示モード | 参照専用（編集不可） |
| 共有モードの種類 | **①ライブ**：含まれる全セクションが常に最新のプロジェクトデータを反映する閲覧用URL。**②見積バージョン紐付け（`estimateVersion`）**：見積もりセクションのみ、指定した発行済み`estimate_versions`（§4.11）の凍結データを表示する。ディレクトリマップ・スケジュールのセクション（含めている場合）は、このモードでも常に最新（ライブ）のデータを表示する——**凍結されるのは見積金額のみ** |
| デフォルト選択ロジック（確定） | 共有作成画面で見積もりセクションを含む場合、デフォルトは**見積バージョン紐付け**とし、その場でプロジェクトの発行済み`estimate_versions`一覧から紐付けるバージョンを選択させる（未発行の場合は「先に見積もり画面でPDF発行してください」と案内し、発行するまで`estimateVersion`モードは選択不可とする）。見積もりセクションを含まない（スケジュール／ディレクトリマップのみ）場合、デフォルトは**ライブ** |
| マスタ単価変更時の影響（確定） | ライブモードの見積もりセクションは常に最新のマスタ単価を反映する（仕様通りの挙動であり、これ自体は不具合ではない）。マスタの単価・工数を変更して保存する際、影響を受ける「有効なライブ共有リンク（失効・期限切れでなく、見積もりセクションを含むもの）」が存在する場合は、保存前に確認ダイアログで対象件数・プロジェクト名を表示する（例：「この変更は現在有効な3件のライブ共有（見積もり公開中）の表示金額に影響します。続行しますか？」）。**見積バージョン紐付けモードの共有リンクは、参照する`estimate_versions`が発行時点で凍結されているためこの警告の対象外であり、マスタ変更の影響を一切受けない**（§4.11） |
| セキュリティ（確定） | URLは推測不可能なランダムトークンを含む。パスワードは任意設定（必須化はしない。案件の機密度に応じて発行者が都度判断する）。有効期限は発行時に日数指定（デフォルト90日、発行者が変更可能）。発行者はいつでも失効できる |
| アクセスログ | 誰が発行したか、閲覧回数・最終閲覧日時を記録する |
| 複数発行 | 1プロジェクトに対し用途別に複数の共有URLを発行・個別管理できる |
| ダウンロード（Phase 12で追加） | メタ情報セクションを含む場合、開発者向けCSVをダウンロードできる。見積もりセクションを含み`estimateVersion`モードの場合、発行済み見積書PDFをダウンロードできる（§4.11） |

> ~~⚠️ 要相談：共有URL発行時のデフォルト選択モード~~ → 解決：見積もりセクションの有無で自動判定する（上表参照）。
> ~~⚠️ 要相談：パスワード設定の必須化の是非~~ → 解決：必須化しない。任意設定とし、発行者の判断に委ねる（上表参照）。

### 4.11 見積書PDF出力・バージョン管理（新規要件）

見積もりを正式な書面として発行し、発行後はマスタ単価が変更されても内容が変わらないようにする。

| 項目 | 内容 |
|---|---|
| 発行操作 | 見積もり画面（§4.7）に「確定してPDF発行」ボタンを設置。押下時点の計算結果（ディレクション費・ページ別コスト・**追加項目（素材費・値引き等）**・小計・消費税・合計）を`estimate_versions`（§6）として凍結保存し、同時にPDFを生成する |
| 見積書の体裁 | 見積番号（自動採番）・発行日・有効期限（マスタ設定の`estimate_validity_days`から算出。既定30日）・御中（クライアント名）・件名（プロジェクト名）・**①ディレクション費 → ②ページ別内訳 → ③追加項目の内訳（項目名・金額）の順**・小計／消費税／合計・自社情報（会社名・住所・電話番号・角印画像）を含む正式な体裁とする（表示順は§4.7参照） |
| 生成方式 | `@react-pdf/renderer`によるPDFネイティブ生成（Vercelのサーバーレス関数上で実行） |
| 発行元情報 | マスタ設定に発行元情報欄（会社名・住所・電話番号・角印画像）を追加する（§4.4・§6） |
| バージョン一覧 | 見積もり画面に、そのプロジェクトで発行済みの`estimate_versions`を一覧表示する（見積番号・発行日・有効期限・合計金額・発行者）。各行からPDFを再ダウンロードできる |
| 不変性（確定） | 発行済みの`estimate_versions`はイミュータブルとして扱う。発行後にマスタの単価・工数・消費税率を変更しても、既存の`estimate_versions`のデータ・PDFの内容は一切変わらない |
| 出力範囲（Phase 12で変更） | PDF発行・再ダウンロードは社内の見積もり画面から行える。外部共有閲覧画面（§4.10）からも、`estimateVersion`モード（発行済みPDFが存在する場合）に限りPDFダウンロードを提供する。`live`モードは都度PDF生成の仕組みを持たないため対象外（画面上での金額閲覧のみ） |
| 外部共有との連携 | 外部共有リンクのモード`estimateVersion`（§4.10）で発行済みバージョンのいずれかを指定すると、その共有リンクの見積もりセクションは指定バージョンの凍結データを表示し、発行済みPDFをダウンロードできる |

---

## 5. 画面一覧と遷移

**共通レイアウト**：左固定サイドバー＋右メインエリア（§12）。ログイン前画面のみ中央カード1枚のレイアウト。

| No. | 画面名 | 内容 |
|---|---|---|
| 1 | ログイン画面 | Googleアカウントでログイン（`marketingdept-llc.com` ドメイン制限） |
| 2 | プロジェクト選択・基本情報 | プロジェクト一覧・作成・切替・削除。クライアント名／担当者／サーバーリンク／Figmaリンクの編集 |
| 3 | ディレクトリマップ | ページ管理・進行グループ設定・ツリー表示 |
| 4 | スケジュール | ページ別ガントチャート・手動編集・並行作業人数設定 |
| 5 | 見積もり | ページ別コスト・ディレクション費・合計・CSVエクスポート・**確定してPDF発行・バージョン一覧（§4.11）** |
| 6 | メタ情報・進行管理 | SEOメタ情報・進捗ステータス編集、**AI一括生成パネル（§4.9）** |
| 7 | マスタ設定 | 単価・工数・休日カレンダー・並行作業人数・AI連携設定・**発行元情報（§4.11）**（チーム共有） |
| 8 | 外部共有リンク管理 | 発行・失効・アクセス状況確認 |
| 9 | 共有閲覧画面（社外向け・ログイン不要） | 共有URLへのアクセス時、必要に応じてパスワード入力を求め、選択セクションのみ参照専用表示 |

---

## 6. データモデル（Supabase / Postgres）

```
master（チーム共有・1件のみ）
  id                       uuid pk
  rates                    jsonb   -- 複雑度 × コスト工程 → { days, cost }
  standards                jsonb   -- スケジュール工程 → { checkback, buffer }
  weekly_off               int[]   -- 曜日番号の配列（デフォルト [0,6]）
  holidays                 jsonb   -- [{ date, label }]
  direction_monthly_rate   numeric
  default_parallel_by_phase jsonb -- スケジュール工程 → number
  tax_rate                 numeric
  issuer_company_name      text    -- 見積書発行元の会社名（§4.11）
  issuer_address           text    null
  issuer_phone             text    null
  issuer_stamp_image_url   text    null  -- 角印画像（Supabase Storage）
  estimate_validity_days   int     default 30  -- 見積書の有効期限（発行日からの日数）
  ai_api_key               text    -- Claude APIキー（暗号化して保存）
  ai_model                 text    -- 使用モデル名
  updated_at               timestamptz

projects
  id                       uuid pk
  client_name              text
  project_name             text
  start_date               date
  parallel_by_phase        jsonb   -- マスタ既定値の上書き
  created_at               timestamptz
  updated_at               timestamptz

project_owners
  id                       uuid pk
  project_id               uuid fk -> projects.id
  role                     text    -- ディレクター/デザイナー/コーダー等
  name                     text

project_links             -- サーバー情報リンク・Figmaリンク共通（§4.1）
  id                       uuid pk
  project_id               uuid fk -> projects.id
  category                 text    -- 'server' | 'figma'
  label                    text
  url                      text
  sort_order               int

progress_groups
  id                       uuid pk
  project_id               uuid fk -> projects.id
  name                     text
  sort_order               int     -- 1が最初に着手するグループ（起点計算に使用。§4.3）

pages
  id                       uuid pk
  project_id               uuid fk -> projects.id
  name                     text
  type                     text    -- 'top' | 'lower' | 'lp' | 'blog' | 'other'
  complexity               text    -- 'S' | 'M' | 'L'
  parent_id                uuid fk -> pages.id  null
  wire_needed              boolean
  copy_needed              boolean
  extra_cost               numeric
  group_id                 uuid fk -> progress_groups.id  null
  priority                 int
  slug                     text    null
  title                    text    null
  description              text    null
  keywords                 text    null
  due_date                 date    null
  status                   text    -- '未着手'|'構成中'|'デザイン中'|'コーディング中'|'テスト中'|'公開済'
  created_at               timestamptz
  updated_at               timestamptz

estimate_line_items        -- 見積もりの手入力追加項目（素材費・値引き等、§4.7）
  id                       uuid pk
  project_id               uuid fk -> projects.id
  label                    text    -- 項目名（例: 素材費、初回割引 等）
  amount                   numeric -- 金額。正負自由入力（値引きは負の値で入力する）
  sort_order               int
  created_at               timestamptz
  updated_at               timestamptz

schedule_overrides         -- 手動編集の記録（§4.6）
  id                       uuid pk
  page_id                  uuid fk -> pages.id
  phase_key                text    -- '構成'|'デザイン'|'コーディング'|'テストアップ'|'公開'
  override_start           date
  override_end             date
  cascade_following        boolean
  edited_by                uuid fk -> auth.users.id
  edited_at                timestamptz

ai_meta_generation_logs    -- AI一括生成の記録（§4.9）
  id                       uuid pk
  project_id               uuid fk -> projects.id
  instruction              text
  scope                    text    -- 'empty_only' | 'all'
  page_count               int
  generated_by             uuid fk -> auth.users.id
  generated_at             timestamptz

estimate_versions           -- 見積書バージョン管理・PDF発行（§4.11）
  id                       uuid pk
  project_id               uuid fk -> projects.id
  quote_number             text unique  -- 見積番号（自動採番。例：EST-20260801-0001）
  version_number           int     -- プロジェクト内の発行連番（1, 2, 3...）
  issued_at                timestamptz
  valid_until              date    -- issued_at + master.estimate_validity_days から算出
  estimate_data            jsonb   -- 発行時点の凍結データ（表示順は §4.7 の①②③に対応）： { directionFee, pages: [{ pageId, pageName, cost }], lineItems: [{ label, amount }], subtotal, taxRate, taxAmount, total }
  pdf_url                  text    -- Supabase Storage（非公開バケット）に保存したPDFファイルのパス
  created_by               uuid fk -> auth.users.id
  created_at               timestamptz

share_links                -- 外部共有（§4.10）
  id                       uuid pk
  project_id               uuid fk -> projects.id
  token                    text unique  -- 推測不可能なランダム文字列
  mode                     text    -- 'live' | 'estimateVersion'
  estimate_version_id      uuid fk -> estimate_versions.id  null  -- modeが'estimateVersion'の場合のみ設定。見積もりセクションはこのバージョンの凍結データを表示する
  include_sections         jsonb   -- { basicInfoPublic, basicInfoFull, estimate, directoryMap, schedule, meta }。basicInfoPublicとbasicInfoFullは排他（同時にtrueにしない）
  password_hash            text    null
  expires_at               timestamptz null
  revoked                  boolean default false
  created_by               uuid fk -> auth.users.id
  created_by_email         text    null  -- 発行者メールのスナップショット（auth.usersを直接JOINしないため）
  created_at               timestamptz
  view_count               int default 0
  last_viewed_at           timestamptz null
```

- **RLS**：`master` / `projects` / `project_owners` / `project_links` / `progress_groups` / `pages` / `estimate_line_items` / `schedule_overrides` / `ai_meta_generation_logs` は、`marketingdept-llc.com` ドメインの認証済みユーザーのみ読み書き可能（権限分離なし、§2）。`share_links` はチームメンバーのみ読み書き可能。共有閲覧画面（`share_view`）からのアクセスはService Role経由のサーバーサイド処理でトークン・パスワード照合を行い、クライアントから直接テーブルを読ませない。
- `master.ai_api_key` はアプリ層で暗号化して保存する（環境変数の暗号鍵を使用。鍵管理はサーバー管理システムの`crypto.php`相当の方針をNext.js側で踏襲）。
- **列単位の秘匿（重要・確定）**：RLSはテーブル単位の許可であり、列単位の秘匿は保証しない。`master.ai_api_key`は、クライアントに返る一切のSELECT（マスタ設定画面の表示・APIレスポンス含む）から明示的に除外する（`select('id, rates, standards, ...')`のように返す列を列挙し、`select('*')`は使わない）。マスタ設定画面では実際の値を返さず、「未設定」／「設定済み（下4桁のみ表示、例：`****ab12`）」のマスク表示のみ行う。Claude API呼び出し時の復号・利用は必ずサーバーサイド（Route Handler / Server Action、Supabase Service Role経由）に閉じ、クライアントバンドルやネットワークレスポンスに生の値を含めない。

---

## 7. 技術スタック

- フロント／バックエンド：**Next.js（App Router）+ TypeScript**
- DB／認証／ストレージ：**Supabase**（Postgres + Auth + RLS）
- ホスティング：**Vercel**
- 認証：Supabase Auth の Google OAuth プロバイダ。ログイン後、メールアドレスのドメインが `marketingdept-llc.com` であることをサーバーサイドで検証し、不一致なら強制ログアウト・アクセス拒否する
- スタイル：Tailwind CSS（`DESIGN_SYSTEM.md` のトーンをトークン化、§12）
- フォント：Noto Sans JP（本文）／ Archivo（英字ラベル・ロゴ）。DESIGN_SYSTEM.md を踏襲
- AI連携：Claude API（Anthropic）。構造化出力（JSON）でメタ情報一括生成を実装（§4.9）
- PDF生成：`@react-pdf/renderer`によるPDFネイティブ生成（見積書PDF、§4.11）。生成したPDFはSupabase Storageの非公開バケットに保存する

---

## 8. 非機能要件

- ログイン認証必須（単価情報を含むため）。Supabase Auth + Google OAuth、ドメイン制限。
- チーム共有データ（マスタ・プロジェクト一覧）は同一組織内の全メンバーがアクセス可能。編集権限の細分化は次フェーズで検討。
- レスポンシブ対応は必須としない（社内利用のデスクトップ運用を主とする）が、タブレット幅での閲覧に配慮する。
- データのエクスポート（CSV等）を各画面に用意する。
- 外部共有URLは推測不可能なトークンを用い、パスワード設定・有効期限・失効操作を機能として備える。共有URL経由のアクセスは編集権限を一切持たない参照専用とする。
- Claude APIキーはマスタ設定内で暗号化して保存し、画面上には再表示しない（登録・更新のみ可能。§6の列単位除外ルールを参照）。
- **タイムゾーン（確定）**：営業日計算（休日カレンダー・チェックバック・バッファの日数加算、進行グループの起点計算等）はすべて **JST（Asia/Tokyo）** を前提とする。Vercel関数・Supabaseのサーバー既定タイムゾーンはUTCのため、日付はタイムゾーンなしの`date`型（`YYYY-MM-DD`文字列）として一貫して扱い、タイムゾーン変換を伴う`Date`オブジェクトでの日時演算は避ける（UTCとのずれによる1日ズレを防止する。サーバー情報管理アプリでも同種のバグが発生し、`Asia/Tokyo`固定で解消した実績あり）。「今日」の判定が必要な箇所（起算日確定等）も、サーバーのUTC時刻からJSTへ変換した上で日付を確定する。
- 発行済み見積書PDF・角印画像はSupabase Storageの非公開バケットに保存し、ダウンロードは認証済みユーザー限定の署名付きURL経由とする（外部共有閲覧画面からは提供しない、§4.11）。
- 想定同時利用人数・データ量（案件数・ページ数の上限目安）は実装前にヒアリングして確定する。

---

## 9. v1スコープ境界

**スコープ内**（要件定義書§4.1 + 本書追加分）：ディレクトリマップ、進行グループ設定、マスタ設定、スケジュール自動生成・手動編集、見積もり自動算出、見積書PDF発行・バージョン管理（新規）、メタ情報・進行管理、AIによるメタ情報一括生成（新規）、プロジェクト基本情報管理（サーバー情報リンク・Figmaリンク含む）、外部共有機能、チーム共有。

**スコープ外**：フォーム自動返信メールの文言管理、実際のワイヤーフレーム・コピー・デザインデータそのものの生成（AIによる制作物自動生成は行わない。メタ情報のみAI対象）、Figma／WordPress管理画面等との自動連携（リンク登録のみ）、請求書発行・会計連携。

---

## 10. 未決定事項（実装前に確認すべき事項）

| # | 論点 | 現時点の仮方針 |
|---|---|---|
| 1 | テストアップ・公開のページ単位要否フラグの要否（要件定義書§10-#5） | 未実装。必要なら追加要件として起票 |
| 2 | ~~共有URL発行時のデフォルト選択モード（ライブ／スナップショット）~~ | 解決：見積もりセクションの有無で自動判定（§4.10） |
| 3 | ~~共有URLの有効期限デフォルト値・パスワード必須化の是非~~ | 解決：デフォルト90日（発行時に変更可）、パスワードは任意設定・必須化しない（§4.10） |
| 4 | Claude API利用コストの妥当性 | 実装後に実測して確認（§4.9） |
| 5 | 想定同時利用人数・データ量の上限目安 | 実装前にヒアリングして確定 |

---

## 11. 受け入れ基準（v1完了の定義）

- [ ] Googleアカウント（`marketingdept-llc.com` ドメイン）でログインでき、他ドメインのアカウントは拒否される
- [ ] ディレクトリマップでページを追加・編集し、親子階層・進行グループ・優先度が正しくツリー表示される
- [ ] 進行グループの開始条件が§4.3の逐次計算ロジック通りに動作する（グループ1＝プロジェクト開始日起点、グループ2以降＝前グループの構成完了日起点）
- [ ] スケジュールが営業日ベース・工程別レーン制約に基づき自動生成される
- [ ] ガントチャートの手動編集が可能で、オーバーライド区間が自動再計算で上書きされない。後続工程の追従要否を都度選択できる
- [ ] 見積もりがページ別コスト・ディレクション費（暦月カウント）・税抜/税込合計で自動算出され、CSVエクスポートできる
- [ ] 見積もりに項目名・金額（正負自由入力）の追加項目（素材費・値引き等）を手入力で追加でき、合計計算・CSV・PDFに反映される。PDF発行後にマスタ単価や追加項目の内容を変更しても、発行済みバージョンの追加項目データは変わらない
- [ ] 見積もり画面で「確定してPDF発行」を実行すると、見積番号・発行日・有効期限・自社情報（会社名・住所・電話番号・角印）を含む見積書PDFが発行され、過去バージョン一覧から再ダウンロードできる
- [ ] PDF発行後にマスタ単価を変更しても、発行済みPDF（および対応する`estimate_versions`のデータ）の内容が変わらないこと
- [ ] メタ情報・進行管理画面でSEO情報・進捗ステータスを編集できる
- [ ] AI一括生成パネルで指示文を入力し「生成」を押すと、全ページ分のslug/title/description/keywords案がプレビュー表示され、確認・修正の上で反映できる。「未入力のみ」選択時に既存の手入力値が上書きされない
- [ ] プロジェクト基本情報画面でサーバー情報リンク・Figmaリンクをそれぞれ複数・ラベル付きで登録できる
- [ ] 外部共有URLを発行でき、マスタ設定を含まない範囲でクライアント/外部スタッフが参照専用で閲覧できる。パスワード・有効期限・失効が機能する。見積もりセクションを含む場合はデフォルトが見積バージョン紐付け（`estimateVersion`）になり、発行済みバージョンの金額が固定表示される（ディレクトリマップ・スケジュールは引き続きライブ表示）
- [ ] 構成工程のオーバーライドがグループの完了日（次グループの起点）を変える場合、後続グループの自動計算区間を再計算するか確認ダイアログで選択でき、既存のオーバーライド区間は再計算の影響を受けない
- [ ] マスタの単価・工数を変更する際、影響を受ける有効なライブ共有リンク（見積もり公開中）がある場合は保存前に警告表示される
- [ ] マスタ設定画面でAPIキーは常にマスク表示され、生の値がAPIレスポンス・画面のどこにも含まれない
- [ ] 営業日計算がJSTで一貫しており、UTCとのズレによる日付のずれが発生しない
- [ ] `DESIGN_SYSTEM.md` のトーン（配色・サイドバー構成・情報密度）に沿ったUIになっている

---

## 12. ビジュアルアイデンティティ（デザイン）

**視覚の基準ファイル：`DESIGN_SYSTEM.md`**（名刺情報共有ツール／サーバー情報管理アプリの共通デザインシステム）のトーンを踏襲する。実装は素HTML/CSS前提で書かれているため、Next.js + Tailwindへの移植が必要。

- **移植方針**：`DESIGN_SYSTEM.md` §1〜§5のCSS変数・コンポーネント仕様（カラーパレット、タイポグラフィ、スペーシング・角丸・シャドウ、レイアウトシェル、コンポーネント）をTailwindの`theme`（`tailwind.config`のcolors/fontFamily/borderRadius等）とReactコンポーネント（サイドバー・ページ見出し・ボタン・フォーム・テーブル・タグ・パネル）に置き換える。
- **配色**：`--navy` / `--accent` は本ツール用に変更してよいが、グレースケール・danger色は共通のまま流用する（`DESIGN_SYSTEM.md`§1の方針通り）。
- **レイアウト**：左固定サイドバー＋右メインエリア。モバイル幅（768px以下）では上部バー＋ハンバーガーメニューに変形する構成を踏襲する（`DESIGN_SYSTEM.md`§4.1）。
- **タイポグラフィ**：日本語本文はNoto Sans JP、英字ラベル・ロゴはArchivo。
- **情報密度**：業務ツールとして高密度なテーブル・詳細画面（ディレクトリマップのツリー、ガントチャート、見積もり内訳等）を志向し、マーケティングサイト的な余白の広さは狙わない。
- 実装後はヘッドレスブラウザ等でPC幅・タブレット幅のスクリーンショットを確認し、崩れがないことを確認してから完了とする。
